# Production Environment Configuration (Future AWS Setup)

# Database Configuration (RDS or dedicated instance)
db.host=prod-db-host
db.port=5432
db.username=prod_admin
db.password=CHANGE_ME_IN_PRODUCTION

# Eureka Configuration
eureka.host=prod-eureka-server
eureka.port=8761

# JWT Configuration (MUST be changed in production)
jwt.secret=CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET

# CORS Configuration (production domains only)
cors.allowed.origins=https://splitsavers.com,https://www.splitsavers.com,https://api.splitsavers.com

# File Upload Configuration
file.base.url=https://api.splitsavers.com

# API Gateway
api.gateway.host=prod-api-gateway
api.gateway.port=8080

# Production-specific settings
prod.domain=splitsavers.com
